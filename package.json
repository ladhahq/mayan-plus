{
  "name": "mayan-plus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run copy-assets",
    "copy-assets": "cp -r laya.js utils.min.js jump3d.max.js version.json unpack.json Role.ani JumpEffect_*.ani LayaScene_* game home settle sound font res dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2"
  }
}
